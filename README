Repository for the University of Waterloo Alternative Fuels Team HMI software.

canbusd uses a Kvaser Memorator CAN logger to watch the CAN bus.
hmi.py is used to start the HMI
gui/ contains
	gui.py: the GUI for the HMI
	canparse.py: a library to parse specific CAN messages to useful values
	config: configuration file for display
		Format: 
			label 	(t): page#,t,text,handler
			number	(n): page#,n,text,units,startbit,length,scalefactor
			bool	(b): page#,b,text,bit
